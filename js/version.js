---
---
// GitHub Pages (Jekyll) fills in these values on every deploy. Keep the two dashed lines at the
// top: they tell Jekyll to process this file. Opened locally, this file isn't valid JavaScript,
// so the version badge shows "Local copy" instead.
export const COMMIT = '{{ site.github.build_revision }}';
export const BUILT = '{{ site.time | date_to_xmlschema }}';
export const REPO = '{{ site.github.repository_nwo }}';
